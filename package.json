{
  "name": "thai-chuay-kid",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "THAI ช่วยคิด — client-side React PWA for calculating and tracking ไทยช่วยไทย 60/40 co-payment spending.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.2",
    "gh-pages": "^6.1.1",
    "vite": "^5.4.8",
    "vite-plugin-pwa": "^0.20.5"
  }
}
